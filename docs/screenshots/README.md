# Screenshot Gallery

These screenshots show the expected shape of the Supabase IDE interface.

## Connection Setup

![Connection setup screen](connection-setup.png)

The initial setup screen appears when no Supabase connections are saved locally.

## Table Browser

![Table browser with saved tables, column metadata, and header filters](table-browser.png)

The main table browser shows saved tables, discovered column types, sortable column headers, and header filters.

## Visual Filters

![Visual filter builder with synchronized header filters](visual-filters.png)

The visual filter builder uses the same filter state as the table header controls.

## Advanced Query

![Advanced query editor with explicit Run action and request URL preview](advanced-query.png)

The advanced query editor submits only through the Run action or keyboard submit, while the request URL preview remains visible.
